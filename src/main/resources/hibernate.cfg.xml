<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>

        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/userdb</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">postgres</property>

        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <property name="hibernate.show_sql">true</property> <!-- Показывает SQL-запросы в консоли -->
        <property name="hibernate.format_sql">true</property> <!-- Делает SQL (с переносами) -->

        <property name="hibernate.hbm2ddl.auto">update</property> <!-- обновляет таблицы автоматически -->

        <mapping class="com.aston.model.User"/> <!-- класс, который нужно связать с таблицей в БД -->

    </session-factory>
</hibernate-configuration>